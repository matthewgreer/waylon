function tharSheBlows(ctx) {
  this.ctx = ctx;
}

tharSheBlows.prototype.draw = function draw(ctx) {

  // tharsheblows/breath
  ctx.save();
  ctx.beginPath();
  ctx.moveTo(
    x + 326 * scale, 
    11
  );
  ctx.bezierCurveTo(
    x + 318 * scale, 
    4, 
    x + 306 * scale, 
    3, 
    x + 297 * scale,
    9
  );
  ctx.bezierCurveTo(
    x + 297 * scale,
    10,
    x + 296 * scale,
    10,
    x + 295 * scale,
    10
  );
  ctx.bezierCurveTo(
    x + 294 * scale,
    11,
    x + 293 * scale,
    10,
    x + 292 * scale,
    10
  );
  ctx.bezierCurveTo(
    x + 285 * scale, 
    7, 
    x + 276 * scale, 
    7, 
    x + 268 * scale, 
    10
  );
  ctx.bezierCurveTo(
    x + 264 * scale,
    11,
    x + 260 * scale,
    14,
    x + 261 * scale,
    18
  );
  ctx.bezierCurveTo(
    x + 258 * scale,
    18,
    x + 255 * scale,
    17,
    x + 251 * scale,
    17
  );
  ctx.bezierCurveTo(
    x + 248 * scale,
    17,
    x + 244 * scale,
    17,
    x + 241 * scale,
    19
  );
  ctx.bezierCurveTo(
    x + 238 * scale,
    20,
    x + 236 * scale,
    24,
    x + 237 * scale,
    27
  );
  ctx.bezierCurveTo(
    x + 232 * scale,
    30,
    x + 226 * scale,
    33,
    x + 221 * scale,
    36
  );
  ctx.bezierCurveTo(
    x + 217 * scale,
    37,
    x + 213 * scale,
    39,
    x + 210 * scale,
    43
  );
  ctx.bezierCurveTo(
    x + 208 * scale,
    46,
    x + 207 * scale,
    51,
    x + 209 * scale,
    54
  );
  ctx.bezierCurveTo(
    x + 211 * scale,
    57,
    x + 214 * scale,
    58,
    x + 217 * scale,
    59
  );
  ctx.bezierCurveTo(
    x + 226 * scale,
    62,
    x + 235 * scale,
    65,
    x + 244 * scale,
    68
  );
  ctx.bezierCurveTo(
    x + 241 * scale,
    71,
    x + 242 * scale,
    76,
    x + 244 * scale,
    79
  );
  ctx.bezierCurveTo(
    x + 247 * scale,
    83,
    x + 250 * scale,
    85,
    x + 254 * scale,
    87
  );
  ctx.bezierCurveTo(
    x + 260 * scale,
    89,
    x + 266 * scale,
    91,
    x + 273 * scale,
    91
  );
  ctx.bezierCurveTo(
    x + 274 * scale,
    95,
    x + 278 * scale,
    97,
    x + 282 * scale,
    96
  );
  ctx.bezierCurveTo(
    x + 286 * scale,
    96,
    x + 290 * scale,
    94,
    x + 293 * scale,
    92
  );
  ctx.bezierCurveTo(
    x + 297 * scale,
    91,
    x + 301 * scale,
    90,
    x + 305 * scale,
    92
  );
  ctx.bezierCurveTo(
    x + 308 * scale,
    93,
    x + 313 * scale,
    99,
    x + 311 * scale,
    104
  );
  ctx.bezierCurveTo(
    x + 310 * scale,
    108,
    x + 328 * scale,
    107,
    x + 328 * scale,
    103
  );
  ctx.bezierCurveTo(
    x + 328 * scale,
    98,
    x + 334 * scale,
    99,
    x + 333 * scale,
    94
  );
  ctx.bezierCurveTo(
    x + 339 * scale,
    100,
    x + 349 * scale,
    101,
    x + 358 * scale,
    98
  );
  ctx.bezierCurveTo(
    x + 362 * scale,
    96,
    x + 367 * scale,
    91,
    x + 365 * scale,
    87
  );
  ctx.bezierCurveTo(
    x + 370 * scale,
    86,
    x + 375 * scale,
    87,
    x + 381 * scale,
    87
  );
  ctx.bezierCurveTo(
    x + 386 * scale,
    88,
    x + 392 * scale,
    88,
    x + 396 * scale,
    85
  );
  ctx.bezierCurveTo(
    x + 401 * scale,
    83,
    x + 404 * scale,
    77,
    x + 402 * scale,
    72
  );
  ctx.bezierCurveTo(
    x + 409 * scale,
    71,
    x + 416 * scale,
    72,
    x + 423 * scale,
    71
  );
  ctx.bezierCurveTo(
    x + 430 * scale,
    69,
    x + 437 * scale,
    63,
    x + 436 * scale,
    56
  );
  ctx.bezierCurveTo(
    x + 435 * scale,
    51,
    x + 431 * scale,
    47,
    x + 427 * scale,
    46
  );
  ctx.bezierCurveTo(
    x + 422 * scale,
    44,
    x + 417 * scale,
    44,
    x + 413 * scale,
    45
  );
  ctx.bezierCurveTo(
    x + 415 * scale,
    37,
    x + 417 * scale,
    30,
    x + 419 * scale,
    23
  );
  ctx.bezierCurveTo(
    x + 419 * scale,
    20,
    x + 420 * scale,
    16,
    x + 418 * scale,
    14
  );
  ctx.bezierCurveTo(
    x + 417 * scale,
    12,
    x + 416 * scale,
    11,
    x + 414 * scale,
    10
  );
  ctx.bezierCurveTo(
    x + 404 * scale, 
    6, 
    x + 392 * scale, 
    11, 
    x + 384 * scale, 
    19
  );
  ctx.bezierCurveTo(
    x + 382 * scale, 
    14, 
    x + 380 * scale, 
    10, 
    x + 377 * scale, 
    6
  );
  ctx.bezierCurveTo(
    x + 374 * scale, 
    3, 
    x + 370 * scale, 
    0, 
    x + 366 * scale, 
    0
  );
  ctx.bezierCurveTo(
    x + 361 * scale, 
    0, 
    x + 356 * scale, 
    3, 
    x + 353 * scale, 
    6
  );
  ctx.bezierCurveTo(
    x + 349 * scale, 
    9, 
    x + 346 * scale, 
    13, 
    x + 343 * scale, 
    16
  );
  ctx.bezierCurveTo(
    x + 343 * scale, 
    12,
    x + 340 * scale, 
    8, 
    x + 336 * scale, 
    7
  );
  ctx.bezierCurveTo(
    x + 332 * scale, 
    6, 
    x + 327 * scale, 
    8, 
    x + 325 * scale, 
    12
  );
  ctx.fillStyle = "rgba(255, 255, 255, .6)";
  ctx.fill();

  // tharsheblows/red
  ctx.beginPath();
  ctx.moveTo(x + 367 * scale, 6);
  ctx.bezierCurveTo(
    x + 367 * scale,
    6,
    x + 285 * scale,
    -26,
    x + 283 * scale,
    95
  );
  ctx.lineTo(x + 287 * scale, 95);
  ctx.bezierCurveTo(
    x + 287 * scale,
    95,
    x + 280 * scale,
    -11,
    x + 368 * scale,
    6
  );
  ctx.lineTo(
    x + 367 * scale, 
    6
  );
  ctx.closePath();
  ctx.fillStyle = "rgba(255, 0, 0, .15)";
  ctx.fill();

  // tharsheblows/orange
  ctx.beginPath();
  ctx.moveTo(
    x + 369 * scale, 
    6
  );
  ctx.bezierCurveTo(
    x + 369 * scale,
    6,
    x + 289 * scale,
    -23,
    x + 287 * scale,
    95
  );
  ctx.lineTo(
    x + 291 * scale, 
    95
  );
  ctx.bezierCurveTo(
    x + 291 * scale,
    95,
    x + 287 * scale,
    -10,
    x + 369 * scale,
    7
  );
  ctx.lineTo(
    x + 369 * scale, 
    6
  );
  ctx.closePath();
  ctx.fillStyle = "rgba(255, 164, 0, .15)";
  ctx.fill();

  // tharsheblows/yellow
  ctx.beginPath();
  ctx.moveTo(
    x + 370 * scale, 
    7
  );
  ctx.bezierCurveTo(
    x + 370 * scale,
    7,
    x + 293 * scale,
    -19,
    x + 291 * scale,
    95
  );
  ctx.lineTo(
    x + 295 * scale, 
    95
  );
  ctx.bezierCurveTo(
    x + 295 * scale,
    95,
    x + 292 * scale,
    -10,
    x + 371 * scale,
    7
  );
  ctx.lineTo(
    x + 370 * scale, 
    7
  );
  ctx.closePath();
  ctx.fillStyle = "rgba(255, 255, 0, .15)";
  ctx.fill();

  // tharsheblows/green
  ctx.beginPath();
  ctx.moveTo(
    x + 372 * scale, 
    7
  );
  ctx.bezierCurveTo(
    x + 372 * scale,
    7,
    x + 296 * scale,
    -16,
    x + 295 * scale,
    95
  );
  ctx.lineTo(
    x + 298 * scale, 
    95
  );
  ctx.bezierCurveTo(
    x + 298 * scale, 
    95, 
    x + 292 * scale, 
    -6, 
    x + 372 * scale, 
    7
  );
  ctx.lineTo(
    x + 372 * scale, 
    7
  );
  ctx.closePath();
  ctx.fillStyle = "rgba(0, 255, 0, .15)";
  ctx.fill();

  // tharsheblows/blue
  ctx.beginPath();
  ctx.moveTo(
    x + 372 * scale,
    7
  );
  ctx.bezierCurveTo(
    x + 372 * scale,
    7,
    x + 299 * scale,
    -12,
    x + 297 * scale,
    95
  );
  ctx.lineTo(
    x + 301 * scale, 
    95
  );
  ctx.bezierCurveTo(
    x + 301 * scale, 
    95, 
    x + 294 * scale, 
    1, 
    x + 373 * scale, 
    8
  );
  ctx.lineTo(
    x + 372 * scale, 
    7
  );
  ctx.closePath();
  ctx.fillStyle = "rgba(0, 0, 255, .15)";
  ctx.fill();

  // tharsheblows/indigo
  ctx.beginPath();
  ctx.moveTo(
    x + 374 * scale, 
    8
  );
  ctx.bezierCurveTo(
    x + 374 * scale, 
    8, 
    x + 299 * scale, 
    -6, 
    x + 301 * scale, 
    95
  );
  ctx.lineTo(
    x + 304 * scale, 
    95
  );
  ctx.bezierCurveTo(
    x + 304 * scale, 
    95, 
    x + 300 * scale, 
    2, 
    x + 374 * scale, 
    8
  );
  ctx.lineTo(
    x + 374 * scale, 
    8
  );
  ctx.closePath();
  ctx.fillStyle = "rgba(74, 0, 129, .15)";
  ctx.fill();

  // tharsheblows/violet
  ctx.beginPath();
  ctx.moveTo(
    x + 375 * scale, 
    8
  );
  ctx.bezierCurveTo(
    x + 375 * scale, 
    8, 
    x + 306 * scale, 
    -5, 
    x + 304 * scale, 
    95
  );
  ctx.lineTo(
    x + 307 * scale, 
    95
  );
  ctx.bezierCurveTo(
    x + 307 * scale, 
    95, 
    x + 305 * scale, 
    6, 
    x + 375 * scale, 
    9
  );
  ctx.lineTo(
    x + 375 * scale, 
    8
  );
  ctx.closePath();
  ctx.fillStyle = "rgba(255, 0, 255, .15)";
  ctx.fill();
  ctx.restore();
}

  module.exports = tharSheBlows;